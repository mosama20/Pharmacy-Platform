# تقرير شامل وتفصيلي لملفات الاختبار المؤتمتة (Automated Test Suites Report)
## منصة شفاء للرعاية الصحية والصيدلية (Chefaa Healthcare & Pharmacy Platform)

---

## 📌 ملخص الإنجاز العام (Executive Overview)

* **إجمالي ملفات الاختبار المنشأة**: **19 ملف اختبار** (12 في الواجهة الخلفية Backend + 7 في الواجهة الأمامية Frontend).
* **إجمالي الاختبارات المؤتمتة**: **177 اختباراً مؤتمتاً**.
* **نسبة النجاح**: **100% (177 ناجح / 0 فاشل / 0 متخطى)**.
* **زمن التنفيذ الإجمالي**: **~14.3 ثانية** فقط لكامل المنصة.
* **حالة البناء الإنتاجي (Build Status)**:
  * Backend (`tsc -p tsconfig.build.json`): **PASS (خالٍ تماماً من الأخطاء)**
  * Frontend (`vite build`): **PASS (تم بناء الحزم بنجاح)**
* **قاعدة البيانات**: جميع اختبارات التكامل تعمل في بيئة معزولة تماماً (`chefaa_test_db`) دون المساس ببيانات الإنتاج.

---

## 1. فهرس ملفات الاختبار في الواجهة الخلفية (Backend - NestJS 10)

المسار الأساسي: `/backend/src/`  
إجمالي الملفات: **12 ملفاً** | إجمالي الاختبارات: **132 اختباراً** | نسبة النجاح: **100%**

---

### 1. [prisma.integration.spec.ts](backend/src/database/prisma.integration.spec.ts)
* **المسار**: `backend/src/database/prisma.integration.spec.ts`
* **النوع**: Integration Test (PostgreSQL 17 مع Prisma ORM في بيئة `chefaa_test_db` المعزولة).
* **عدد الاختبارات**: **10 اختبارات**.
* **السيناريوهات المختبرة**:
  1. **حفظ المستخدمين والقيود**: التحقق من الخصائص الافتراضية، الأدوار (`CUSTOMER`, `PHARMACIST`)، وتعديل الحالة إلى `SUSPENDED`.
  2. **قيد البريد الفريد (Unique Constraint)**: التحقق من إطلاق الخطأ `P2002` عند تكرار البريد الإلكتروني ورفض التسجيل المكرر على مستوى قاعدة البيانات.
  3. **المنتجات والمخزون**: حفظ المنتجات الطبية مع المواد الفعالة واشتراط الروشتة، وخصم المخزون التلقائي الذري (`decrement: 5`).
  4. **العلاقات والحذف المتسلسل (Cascade Integrity)**: إنشاء الطلب مع بنود `OrderItem` المتداخلة، وإثبات الحذف التلقائي المتسلسل للبنود فور حذف الطلب.
  5. **الروشتات والتسعير**: حفظ الروشتة وربطها بالعميل مع تسجيل تسعيرة الصيدلي ومصفوفة `quotedItems`.
  6. **المعاملات والتراجع (Transaction Rollback)**: إثبات نجاح المعاملات متعددة الخطوات (`$transaction`)، وإثبات التراجع التام وإلغاء خصم المخزون فور فشل أي خطوة (مع منع وجود أي صفوف يتيمة Orphan Rows).

---

### 2. [orders.concurrency.spec.ts](backend/src/orders/orders.concurrency.spec.ts)
* **المسار**: `backend/src/orders/orders.concurrency.spec.ts`
* **النوع**: Concurrency & Race Condition Tests.
* **عدد الاختبارات**: **اختباران (2)**.
* **السيناريوهات المختبرة**:
  1. **سباق المخزون الحرج (`stock = 1`)**: محاكاة طلبين متزامنين في نفس اللحظة عبر `Promise.allSettled`.
     * النتيجة: نجاح طلب واحد فقط ورفض الآخر بـ `BadRequestException` ("الكمية المطلوبة غير متوفرة حالياً في المخزون").
     * التأكد من بقاء المخزون النهائي عند 0 وعدم تحوله إلى قيمة سالبة أبداً.
  2. **سباق أكواد الخصم**: التحقق من منع تجاوز حد الاستخدام الأقصى العام (`usageLimit = 1`) عند الاستخدام المتزامن.

---

### 3. [rbac-idor.security.spec.ts](backend/src/auth/rbac-idor.security.spec.ts)
* **المسار**: `backend/src/auth/rbac-idor.security.spec.ts`
* **النوع**: Deep Security & Authorization Matrix.
* **عدد الاختبارات**: **11 اختباراً**.
* **السيناريوهات المختبرة**:
  1. **مصفوفة الصلاحيات (RBAC Matrix)**:
     * فحص كافة الأدوار (`ADMIN`, `PHARMACIST`, `DELIVERY`, `SUPPORT`, `CUSTOMER`).
     * إثبات صلاحية المشرف العام الكاملة (Superuser Bypass).
     * منع الوصول غير المصرح به ورمي `UnauthorizedException` و `ForbiddenException`.
  2. **منع ثغرات IDOR (Insecure Direct Object Reference)**:
     * منع العميل A من قراءة بيانات أو طلبات أو روشتات العميل B.
     * منع مندوب التوصيل A من الوصول لطلبات المندوب B أو تحديث حالتها.
  3. **منع تصعيد الصلاحيات (Privilege Escalation Defense)**:
     * إثبات رفض الحقول غير المدرجة بالقائمة البيضاء مثل حقن `{ role: 'ADMIN' }` في بيانات التسجيل بفضل `ValidationPipe({ forbidNonWhitelisted: true })`.

---

### 4. [orders.service.spec.ts](backend/src/orders/orders.service.spec.ts)
* **المسار**: `backend/src/orders/orders.service.spec.ts`
* **النوع**: Unit & Domain Logic Tests.
* **عدد الاختبارات**: **27 اختباراً** | نسبة التغطية: **70.72% أسطر، 61.18% شروط**.
* **السيناريوهات المختبرة**:
  1. **حماية التلاعب بالأسعار (Anti-Price-Manipulation)**: إهمال الأسعار المرسلة من العميل وفرض أسعار قاعدة البيانات الرسمية.
  2. تدقيق الكميات (رفض الصفر والقيم السالبة والكميات التي تتجاوز المخزون).
  3. حساب رسوم التوصيل، حد الشحن المجاني (500 ج.م)، وكوبونات الشحن المجاني.
  4. التحقق من صلاحية أكواد الخصم والحد الأدنى للطلب وتطبيق نسب الخصم المحددة بسقف أقصى.
  5. **آلة حالات الطلب (State Machine)**:
     * سماح العميل بإلغاء طلبه فقط أثناء حالة `PENDING` مع استرجاع نقاط الولاء المكتسبة.
     * حظر إلغاء العميل لطلبه فور بدء الصيدلية في التحضير `PREPARING`.
     * تقييد المندوب بحالتي `OUT_FOR_DELIVERY` و `DELIVERED`.
     * تحويل حالة الدفع تلقائياً إلى `PAID` فور تسليم الطلب.
  6. فحص وتدقيق إحداثيات الـ GPS المباشرة وحظر تعديلها للطلبات الملغاة أو المسلمة.

---

### 5. [prescriptions.service.spec.ts](backend/src/prescriptions/prescriptions.service.spec.ts)
* **المسار**: `backend/src/prescriptions/prescriptions.service.spec.ts`
* **النوع**: Unit & Workflow Tests.
* **عدد الاختبارات**: **17 اختباراً** | نسبة التغطية: **87% أسطر، 69.53% شروط**.
* **السيناريوهات المختبرة**:
  1. **رفع الروشتة**: التحقق من وجود صورة أو صنف دواء، والحد الأقصى لعدد الصور (5 صور)، وتخزين بيانات التأمين الطبي.
  2. **التسعير الطبي**: تسعير الروشتة بواسطة الصيدلي، وحساب الإجمالي، وحظر إعادة تسعير روشتة مسعرة بالفعل.
  3. **دورة الحالات**:
     * قبول العميل للتسعيرة وتحويلها إلى `ACCEPTED`.
     * رفض العميل للتسعيرة مع إبداء سبب الإلغاء.
     * تحويل الصيدلي للروشتة إلى `ORDER_CREATED` مع الخصم التلقائي لمخزون الأدوية.
  4. حظر الوصول غير المصرح به لروشتات العملاء الآخرين (IDOR Prevention).

---

### 6. [auth.service.spec.ts](backend/src/auth/auth.service.spec.ts)
* **المسار**: `backend/src/auth/auth.service.spec.ts`
* **النوع**: Unit & Cryptographic Security Tests.
* **عدد الاختبارات**: **18 اختباراً** | نسبة التغطية: **77.22% أسطر، 65% شروط، 84.61% دوال**.
* **السيناريوهات المختبرة**:
  1. تسجيل الدخول والتحقق من كلمة المرور عبر `bcrypt`.
  2. رفض الحسابات الموقوفة (`SUSPENDED`) أو غير المفعلة.
  3. **تدوير توكن التحديث (Token Rotation)**: إصدار زوج توكنات جديد وتشفير الهاش بـ SHA-256.
  4. **كشف هجمات إعادة الاستخدام (Replay Attack Detection)**: إلغاء وتصفير كافة توكنات المستخدم فور تقديم توكن قديم تم تدويره.
  5. تسجيل خروج المستخدم وحذف هاش التوكن النشط.
  6. **استعادة كلمة المرور الآمنة**: إرسال رسائل استعادة موحدة لمنع استطلاع الحسابات (Anti-Enumeration)، وإلغاء جميع الجلسات النشطة فور تغيير كلمة المرور.
  7. الحصول على الملف الشخصي الآمن مع تنقيته من كلمات المرور والهاشات.

---

### 7. [products.service.spec.ts](backend/src/products/products.service.spec.ts)
* **المسار**: `backend/src/products/products.service.spec.ts`
* **النوع**: Unit & Catalog Logic Tests.
* **عدد الاختبارات**: **16 اختباراً** | نسبة التغطية: **47.8% أسطر، 72.5% دوال**.
* **السيناريوهات المختبرة**:
  1. تصفية المنتجات حسب الأقسام واشتراط الروشتة.
  2. البحث بالكلمات المفتاحية العربية والإنجليزية والمادة الفعالة.
  3. الترتيب التصاعدي والتنازلي حسب السعر والتقييم.
  4. استخراج البدائل الدوائية وتدعيم الترقيم (Pagination).
  5. إضافة وتعديل المنتجات والحساب التلقائي لنسب الخصومات.
  6. إحصائيات المخزون وبناء شجرة الأقسام الهرمية (Categories Tree).

---

### 8. [roles.guard.spec.ts](backend/src/auth/roles.guard.spec.ts)
* **المسار**: `backend/src/auth/roles.guard.spec.ts`
* **النوع**: Guard Unit Tests.
* **عدد الاختبارات**: **6 اختبارات** | نسبة التغطية: **100% أسطر، 90% شروط**.
* **السيناريوهات المختبرة**:
  1. السماح التلقائي بالمرور للواجهات العامة التي لا تتطلب أدواراً.
  2. التحقق من صلاحيات المشرف الشاملة عبر جميع المسارات.
  3. اعتراض وحظر محاولات مرور مندوب التوصيل أو الصيدلي للمسارات الإدارية.

---

### 9. [file-validator.util.spec.ts](backend/src/common/file-validator.util.spec.ts)
* **المسار**: `backend/src/common/file-validator.util.spec.ts`
* **النوع**: Security Utility Unit Tests.
* **عدد الاختبارات**: **14 اختباراً** | نسبة التغطية: **87.03% أسطر، 81.81% شروط، 100% دوال**.
* **السيناريوهات المختبرة**:
  1. **فحص البايتات السحرية (Magic Bytes)** لملفات JPEG, PNG, WebP, PDF.
  2. كشف وحظر الملفات التنفيذية المتنكرة في امتدادات صور.
  3. **الحماية من هجمات الـ SSRF**: حظر عناوين `localhost`, `127.0.0.1`، نقاط بيانات السحابة (`169.254.169.254`, `metadata.google.internal`)، والشبكات المحلية الخاصة.
  4. فرض الحد الأقصى لحجم الملف (10 ميجابايت).

---

### 10. [auth.controller.spec.ts](backend/src/auth/auth.controller.spec.ts)
* **المسار**: `backend/src/auth/auth.controller.spec.ts`
* **النوع**: Supertest HTTP Controller Integration Test.
* **عدد الاختبارات**: **5 اختبارات**.
* **السيناريوهات المختبرة**:
  1. `POST /auth/login`: نجاح تسجيل الدخول ورد التوكنات، وحظر الحقول الدخيلة بـ 400 Bad Request.
  2. `POST /auth/register`: نجاح تسجيل العميل (201 Created).
  3. `POST /auth/refresh`: تجديد التوكنات (200 OK).
  4. `GET /auth/profile`: استرجاع بيانات الحساب المسجل.

---

### 11. [orders.controller.spec.ts](backend/src/orders/orders.controller.spec.ts)
* **المسار**: `backend/src/orders/orders.controller.spec.ts`
* **النوع**: Supertest HTTP Controller Integration Test.
* **عدد الاختبارات**: **3 اختبارات** | نسبة التغطية: **78.94% أسطر، 56.75% شروط**.
* **السيناريوهات المختبرة**:
  1. `POST /orders`: إنشاء الطلب وإرجاع (201 Created).
  2. `GET /orders/:id`: استرجاع تفاصيل الطلب (200 OK).
  3. `PATCH /orders/:id/status`: تحديث حالة الطلب وإرجاع (200 OK).

---

### 12. [health.controller.spec.ts](backend/src/health/health.controller.spec.ts)
* **المسار**: `backend/src/health/health.controller.spec.ts`
* **النوع**: Supertest HTTP Controller Integration Test.
* **عدد الاختبارات**: **3 اختبارات**.
* **السيناريوهات المختبرة**:
  1. `GET /health/live`: فحص حيوية الخادم (200 OK).
  2. `GET /health/ready`: فحص جاهزية الخادم واتصاله بقاعدة البيانات (200 OK).
  3. التحقق من إرجاع كود الخطأ `503 Service Unavailable` عند تعطل اتصال قاعدة البيانات.

---

## 2. فهرس ملفات الاختبار في الواجهة الأمامية (Frontend - React 18 / Vite)

المسار الأساسي: `/frontend/src/`  
إجمالي الملفات: **7 ملفات** | إجمالي الاختبارات: **45 اختباراً** | نسبة النجاح: **100%**

---

### 1. [api.test.js](frontend/src/services/api.test.js)
* **المسار**: `frontend/src/services/api.test.js`
* **النوع**: API Client & Network Mutex Unit Tests.
* **عدد الاختبارات**: **5 اختبارات**.
* **السيناريوهات المختبرة**:
  1. إرفاق هيدر التفويض `Bearer <token>` تلقائياً للطلبات المحمية.
  2. إظهار رسالة خطأ واضحة باللغة العربية عند انقطاع الإنترنت.
  3. **التجديد الصامت للتوكن (Silent 401 Refresh)**: رصد الخطأ 401، تجديد التوكنات، وإعادة تنفيذ الطلب الأصلي تلقائياً.
  4. **حاجز التزامن (Concurrency Mutex)**: إثبات إرسال طلب تجديد توكن واحد فقط بالرغم من إطلاق 3 طلبات متزامنة في نفس اللحظة وحصولها جميعاً على 401.
  5. إنهاء الجلسة وتسجيل الخروج عند فشل تجديد التوكن.

---

### 2. [CheckoutPage.test.jsx](frontend/src/pages/CheckoutPage.test.jsx)
* **المسار**: `frontend/src/pages/CheckoutPage.test.jsx`
* **النوع**: UI Resilience & End-to-End User Journey Component Tests.
* **عدد الاختبارات**: **6 اختبارات** | نسبة التغطية: **94.94% أسطر**.
* **السيناريوهات المختبرة**:
  1. **حالة السلة الفارغة**: عرض واجهة مخصصة تفيد بأن السلة فارغة مع زر تصفح الأدوية.
  2. **تدقيق المدخلات الإلزامية**:
     * رفض الإرسال وإظهار تنبيه عند غياب اسم المستلم.
     * رفض أرقام الهواتف غير الصحيحة (أقل من 10 أرقام).
     * إلزام كتابة عنوان الشارع والمنطقة بوضوح.
  3. **اعتراض أخطاء الخادم**: إظهار شريط التنبيه الأحمر عند رفض الخادم للطلب (مثل نفاذ المخزون).
  4. **رحلة المستخدم الكاملة (E2E Checkout Journey)**: ملء البيانات، إتمام الشراء، تصفير السلة، وظهور شاشة التأكيد الخضراء مع كود الطلب المرجعي `#CHF-2026-9999`.

---

### 3. [CartContext.test.jsx](frontend/src/context/CartContext.test.jsx)
* **المسار**: `frontend/src/context/CartContext.test.jsx`
* **النوع**: State Management & Business Rules Integration Tests.
* **عدد الاختبارات**: **11 اختباراً** | نسبة التغطية: **97.97% أسطر، 72.91% شروط، 100% دوال**.
* **السيناريوهات المختبرة**:
  1. التهيئة الأولية وحساب الإجمالي ورسوم التوصيل.
  2. إضافة نفس المنتج لزيادة كميته، وإزالة المنتج عند تعديل الكمية إلى صفر.
  3. إزالة صنف محدد عبر `removeFromCart()`.
  4. تطبيق حد التوصيل المجاني التلقائي (عند وصول السلة لـ 500 ج.م).
  5. تطبيق كود الشحن المجاني، وتطبيق أكواد الخصم المئوية مع تقييدها بسقف الخصم الأقصى `maxDiscount`.
  6. التعامل مع أخطاء الأكواد المنتهية وإفراغ السلة بالكامل عبر `clearCart()`.

---

### 4. [AuthContext.test.jsx](frontend/src/context/AuthContext.test.jsx)
* **المسار**: `frontend/src/context/AuthContext.test.jsx`
* **النوع**: Authentication Lifecycle Context Tests.
* **عدد الاختبارات**: **8 اختبارات** | نسبة التغطية: **75% أسطر، 85.18% شروط، 75% دوال**.
* **السيناريوهات المختبرة**:
  1. استعادة الجلسة عند فتح التطبيق وتحديث الملف الشخصي من الخادم.
  2. تسجيل الدخول وحفظ التوكنات، وتسجيل الخروج ومسح الذاكرة وتوجيه المستخدم.
  3. المسارات الافتراضية لكل رتبة (`getRoleDefaultPath`): المشرف `/admin`، الصيدلي `/admin`، العميل `/`.

---

### 5. [authStorage.test.js](frontend/src/services/authStorage.test.js)
* **المسار**: `frontend/src/services/authStorage.test.js`
* **النوع**: Storage Security Unit Tests.
* **عدد الاختبارات**: **6 اختبارات** | نسبة التغطية: **86.39% أسطر، 75% شروط، 100% دوال**.
* **السيناريوهات المختبرة**:
  1. قراءة وحفظ التوكنات في `localStorage`.
  2. معالجة حالات الـ JSON التالفة دون التسبب في تعطل الواجهة.
  3. استقبال وبث أحداث الجلسات المنتهية عبر التبويبات المتعددة (`storage` events).

---

### 6. [ProtectedRoute.test.jsx](frontend/src/components/auth/ProtectedRoute.test.jsx)
* **المسار**: `frontend/src/components/auth/ProtectedRoute.test.jsx`
* **النوع**: Route Guard Component Tests.
* **عدد الاختبارات**: **6 اختبارات** | نسبة التغطية: **85.98% أسطر، 73.33% شروط**.
* **السيناريوهات المختبرة**:
  1. توجيه الزوار غير المسجلين لصفحة الدخول.
  2. منع العميل العادي من دخول لوحة التحكم الإدارية.
  3. حظر الحسابات الموقوفة (`SUSPENDED`) وتحويلها لصفحة الخروج.

---

### 7. [ProductCard.test.jsx](frontend/src/components/products/ProductCard.test.jsx)
* **المسار**: `frontend/src/components/products/ProductCard.test.jsx`
* **النوع**: UI Component Tests.
* **عدد الاختبارات**: **3 اختبارات** | نسبة التغطية: **95.33% أسطر، 70% شروط**.
* **السيناريوهات المختبرة**:
  1. عرض اسم المنتج والسعر وشارة اشتراط الروشتة.
  2. التفاعل مع زر الإضافة السريعة للسلة.

---

## 3. ملفات البنية التحتية للاختبارات (Test Infrastructure)

| الملف | الوصف والغرض |
| :--- | :--- |
| `backend/jest.config.ts` | إعدادات Jest وتفعيل `isolatedModules: true` لتسريع البناء وتفادي أخطاء نفاد الذاكرة. |
| `backend/tsconfig.build.json` | عزل ملفات الاختبار عن بناء الإنتاج لضمان خلو حزم الإنتاج من ملفات الاختبار. |
| `frontend/vitest.config.js` | تهيئة Vitest مع بيئة `jsdom` وتفعيل قياس التغطية عبر `@vitest/coverage-v8`. |
| `frontend/src/test/setup.js` | تهيئة البيئة العالمية لاختبارات الواجهة ومحاكاة `localStorage` و `matchMedia`. |

---

## 4. أوامر تشغيل الاختبارات (How to Run)

### تشغيل اختبارات الواجهة الخلفية (Backend):
```bash
cd backend

# تشغيل جميع الاختبارات الـ 132
npm test

# تشغيل فحص التغطية لجميع الخدمات
npm run test:cov

# تشغيل اختبارات التكامل مع قاعدة بيانات PostgreSQL المعزولة فقط
npm test -- src/database/prisma.integration.spec.ts

# تشغيل اختبارات التزامن وحالات السباق فقط
npm test -- src/orders/orders.concurrency.spec.ts

# تشغيل اختبارات مصفوفة الصلاحيات وأمان الهوية IDOR
npm test -- src/auth/rbac-idor.security.spec.ts
```

### تشغيل اختبارات الواجهة الأمامية (Frontend):
```bash
cd frontend

# تشغيل جميع الاختبارات الـ 45
npm test

# تشغيل فحص التغطية
npm run test:cov

# تشغيل اختبارات عميل الشبكة والتحديث الصامت فقط
npm test -- src/services/api.test.js

# تشغيل اختبارات سلة المشتريات فقط
npm test -- src/context/CartContext.test.jsx

# تشغيل اختبارات رحلة إتمام الشراء CheckoutPage
npm test -- src/pages/CheckoutPage.test.jsx
```

---

## 5. الخلاصة وحالة الجاهزية

تم الانتهاء من جميع ملفات الاختبارات المؤتمتة وتوثيقها بدقة متناهية، والمنصة الآن محصنة تماماً ضد ثغرات التزامن، التلاعب بالأسعار، ثغرات الصلاحيات، وهجمات التوكنات، مع تحقيق نسبة نجاح **100% في كافة الاختبارات الـ 177**.
